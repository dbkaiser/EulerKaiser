#!/usr/bin/python
# though this is useless

def readfile():
	f=file('../test.txt');
	while True:
		str = f.readline();
		print len(str);
		if len(str)==0 :
			break;

	f.close();

readfile();
f= file('../num13.txt');
s = f.readline();
print s;
f.close();

